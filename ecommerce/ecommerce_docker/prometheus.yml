global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

# Kafka metrics would require a JMX exporter. Port 9092 is binary only.
# - job_name: 'kafka'
#   static_configs:
#     - targets: ['kafka:9092']

  - job_name: 'spark'
    static_configs:
      - targets: ['spark-master:8080']
